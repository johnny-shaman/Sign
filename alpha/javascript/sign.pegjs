{
  // 状態管理（Depth）はLexerの前処理に完全に委譲したため、
  // グローバルな変数は一切不要になりました。
}

Start = Program

// 空白・改行の定義
// !! スペースは「見た目の区切り」ではなく「コプロダクト演算子そのもの」である !!
// 1つ以上の半角スペース = Sign言語におけるコプロダクト（余積）演算子
// この「演算子としてのスペース」が Expression 層でフラットリストを生成し、
// Shunting Yard（演算子テーブル）に渡される入力となる。
__ = " "+ { return null; } / &"\x02" { return null; } // coproduct operator（演算子直後に空白なしでインデントブロックが続くケースを先読みで許容（\x02自体は消費しない、Block側が消費する）
_  = " "* { return null; } // optional (used only at line edges)
SOL = &{ return location().start.column === 1; }
EOL = "\r\n" / "\r" / "\n"
EOF = !.

// string_and_comment.md §2「先読みルール」: 行頭のバッククォートは、同じ行に閉じ
// バッククォートが無い場合のみコメントになる（閉じがあれば文字列リテラル）。
// `!([^\r\n\`]* "`")` が「この行の残りのどこかに閉じバッククォートがある」ことを検出する
// 否定先読み。PEGの`*`はバックトラックしない（貪欲に確定したら戻らない）ため、
// 単純に`[^\r\n]* "`"`と書くと`[^\r\n]*`が閉じバッククォート自身も含めて行末まで
// 食い尽くしてしまい、後続の`"`"`が常にマッチできず「閉じは無い」と誤判定し続ける
// （例: `` `123` 123 ``のような閉じあり＝本来は文字列リテラルの行まで誤ってコメントとして
// 飲み込んでいた）。バッククォート自身を文字クラスから除外することで、走査が閉じの
// 位置で自然に止まり、後続の`"`"`がそこにマッチできるようにする。
comment = SOL "`" !([^\r\n`]* "`" [^\r\n]) [^\r\n]* (EOL / EOF) { return null; }

// --- プログラムと行 ---
// コードとコメントが同じファイルに混在できるよう、行ごとに comment / Line のどちらでも
// 受け付ける（以前は「全部コードの行」か「全部コメントの行」かの二者択一だったため、
// 1行でもコメントが混ざると即パースエラーになっていた）。commentは`{ return null; }`
// ——Unit相当——を返すため、asListがUnitをリスト構築からフィルタするのと同じ考え方で、
// 最後にnullを除外する。
Program = lines:(SOL @(comment / Line) EOL*)* EOF { return lines.filter((l) => l !== null); }

Line
  = _ expr:Expression _ { return expr; }

// --- コプロダクト（空白）によるフラットリスト化の核心 ---
// スペース区切りの Term 列を「フラットな配列」として返す。
// この配列が Shunting Yard（演算子テーブル）への入力となる。
// 意味解決（apply/compose/concat の区別）はこの後の意味論フェーズで行う。
Expression
  = head:Term tail:(__ @Term)* {
      // 【修正済み】soloかどうかに関わらず常にflat()する（Termの返り値のwrapルールと対にするため。
      // 以前はsolo時にflat()をスキップしており、Blockが唯一の項の場合とそうでない場合とで
      // 結果の構造が一貫しなかった）。
      return [head, ...tail].flat();
  }

// --- 密着結合（Syntax = Type）の要 ---
// スペースなし = 同一 Term 内 = 「密着」
// これにより、同じ記号でもスペースの有無だけで役割が変わる：
//   @x   → 前置（@_ として記号化）: スペースなしで core の左に密着
//   x @ y → 中置: スペースありで Expression 層のコプロダクト列に入る
//   x@   → 後置（_@ として記号化）: スペースなしで core の右に密着
// 前置記号は末尾に "_" を付与、後置記号は先頭に "_" を付与することで
// Shunting Yard がフラットリスト内での役割を一意に識別できる。
Term
  = pre:Prefixes core:Core post:Postfixes {
      // プレフィックスもポストフィックスもない場合
      if (pre.length === 0 && post.length === 0) {
          // 【修正済み】coreが配列（Block）の場合、Expression側の.flat()で
          // 中身が漏れないよう1階層ラップする。（以前はcoreをそのまま返しており、
          // Blockが他の項と同じExpression内に混在すると flat() で中身が漏れるバグがあった）。
          return Array.isArray(core) ? [core] : core;
      }
      
      // 密着している場合はフラットな配列として返す
      // Expression層の flat() によって全体のリストに溶け込みます
      return [...pre, core, ...post];
  }
  / operator

// 前置演算子には後ろに "_" を付与（例: "@" → "@_"）
// → Shunting Yard が「この @ は前置」と識別できる
Prefixes
  = pre:prefix* { return pre.map(p => p + "_"); }

// 後置演算子には前に "_" を付与（例: "@" → "_@"）
// → Shunting Yard が「この @ は後置」と識別できる
Postfixes
  = post:postfix* { return post.map(p => "_" + p); }

Core
  = Block
  / Atom

// --- 空間的配置（ネスト構造） ---
Block
  = "[" _ exprs:Expressions _ "]" { return exprs; }
  / "{" _ exprs:Expressions _ "}" { return exprs; }
  / "(" _ exprs:Expressions _ ")" { return exprs; }
  // 空ブロック（`[]` / `{}` / `()`）。unit.md「`__ = []`（空リストと等価）」の通り
  // 空リストはUnitと同型であり、`none : []` のように書けなければならない
  // （guide/example.sn 37行目）。Expressionsは1個以上を要求するため、非空の
  // 選択肢の「後ろ」に空専用の選択肢を置いて拾う（PEGは順序付き選択なので、
  // 非空の解釈が先に試される順序は変わらない）。
  / "[" _ "]" { return []; }
  / "{" _ "}" { return []; }
  / "(" _ ")" { return []; }
  // ノルム（要素数）。**2文字の囲み**である——`|` は中置 `or`（tier 6）なので、`|[...]|`
  // では `a | [1 2] | b` と区別がつかない（空白の有無で意味が変わってしまう）。`~|` と
  // `|~` はどの演算子にもなっていない綴りなので、空きを取る。
  //
  // 絶対値（`|x|`）と分けるのは、**1要素の器が存在しない**からである。`[5] ≅ 5` なので
  // `|[5]|` を絶対値と読むと 5、要素数と読むと 1 になり、同じ記号が長さ1で意味を変える
  // ——`count : xs ? |xs|` が `[7]` に対して 7 を返していた。囲みを分ければ、どちらも
  // 曖昧さなく書ける（`|5|` は 5、`~|5|~` は 1）。数学で集合の濃度を `|A|` と書く慣習が
  // 下敷きだが、`|` が埋まっているので二重線ではなく `~` を添える形にした。

  // ノルム（要素数）。**囲みの区別は空白の位置が決める**——絶対値（`|x|`）とまったく
  // 同じ規則である。密着していれば囲み、空白で区切られていれば中置演算子
  // （`||` は tier 22 の `bit_or`）：
  //
  //   ||xs||     ノルム（要素数）
  //   a || b     ビット毎の論理和
  //
  // 絶対値と分けるのは、**1要素の器が存在しない**からである。`[5] ≅ 5` なので `|[5]|` を
  // 絶対値と読むと 5、要素数と読むと 1 になり、同じ記号が長さ1で意味を変える
  // ——`count : xs ? |xs|` が `[7]` に対して 7 を返していた。数学のノルム記法（二重線）に
  // 揃えれば、どちらも曖昧さなく書ける（`|5|` は 5、`||5||` は 1）。
  //
  // **絶対値より先に試す。** 後だと `||5||` が `|` + `|5|` + `|` に読めてしまう。
  / "||" exprs:Expressions "||" &(__ / EOL / EOF / "]" / "}" / ")" / "\x03") { return [`"NORM_"`, exprs]; }
  / "|" exprs:Expressions "|" &(__ / EOL / EOF / "]" / "}" / ")" / "\x03") { return [`"ABS_"`, exprs]; }
  // Lexerが挿入した制御用ASCIIコードによるインデントブロックの切り出し
  // ※実際のLexerの実装に合わせて "\x02", "\x03" は変更してください
  // 【修正済み】以前は ...exprs と展開しており、bracket系(exprsをそのまま返す)より
  // 保護膜が1階層薄く、Expressionの.flat()で他の項と混在した際に中身が漏れる原因に
  // なっていた。exprs を展開せず1つの要素として保持することで、bracket系と同じ
  // 「1階層の保護」に揃える。
  / "\x02" _ exprs:Expressions _ "\x03" { return [`"INDENT_"`, exprs, `"_DEDENT"`]; }

// ブロック内で使われる複数行の式
Expressions
  = head:Expression tail:(EOL _ @Expression)* {
      return [head, ...tail].filter(e => e !== null);
  }

// --- 値（Atom）の定義（既存の正規表現ルールを踏襲） ---
Atom
  = string / charactor / address / register / unicode / number / identifier / unit / hole

string = $("`" [^`\r\n]* "`")
charactor = $("\\".)
number = $("-"? [0-9]+ "."? [0-9]*)
address = $([0-9]+ "x" Hex+)
register = $("0r" Hex+) / $("0b" ("0" / "1")+)
unicode = $([0-9]+ "u" Hex+)
// "__"（Unit）だけは識別子ではなくunitへ回す（&{}述語で除外し、Atomの次の選択肢へフォールスルー）。
// これが無いと "_" [a-zA-Z0-9_]+ が "__" にもマッチしてしまい、unitに一生到達しなくなる。
identifier = id:( $([a-zA-Z][a-zA-Z0-9_]*) / $("_" [a-zA-Z0-9_]+) ) &{ return id !== "__"; } {return `<${id}>`}
Hex = [0-9a-fA-F]
unit = "__" / "\x00"
hole = "_"

// --- 演算子・記号（前置・後置・中置の振る舞い解決はShunting Yardへ） ---
prefix
  = "###" / "##" / "#" / ("-" &(Block / identifier)) / "~" / "!!" / "!" / "$" / "@"

postfix
  = "!" / "~" / "@"

operator
  = $[!"#$%&'-=^~\|@;+:*,<>/?]+
